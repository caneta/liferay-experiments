# Experiment with Liferay AMD Loader and amcharts plugin
## Installation
* Download the repository with git clone
* Run `npm install` to install dependencies
## Run
* Run a minimal server with `npm start`
* Open a web browser with JavaScript console opened
* Go to *http://localhost:3001/index.html*
